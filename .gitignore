# --- Secrets and local environment (never commit; see .env.example) ---
.env
.env.deploy

# --- Per-box rendered state (khctl regenerates these; commit the inputs, not the renders) ---
deploy_plan.json
probe_report.json

# --- Runtime artifacts ---
operator.log
serving_usage.jsonl

# --- Python ---
.venv/
__pycache__/
*.pyc
.pytest_cache/
*.egg-info/
dist/
build/

# --- Pilot corpus and client documents (data, not code) ---
pilot_sops/
reports/
